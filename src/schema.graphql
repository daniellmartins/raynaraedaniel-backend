scalar DateTime

type Query {
  me: User
}

type Mutation {
  signin(code: Int!): AuthPayload
}

type AuthPayload {
  token: String!
  user: User!
}

type User {
  id: ID!
  code: Int!
  name: String!
  lastname: String!
  email: String
  createdAt: DateTime
  updatedAt: DateTime
}
