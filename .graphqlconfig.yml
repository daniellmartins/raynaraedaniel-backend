projects:
  app:
    schemaPath: src/schema.graphql
    extensions:
      endpoints:
        default: http://localhost:4000
  database:
    schemaPath: src/generated/graphql-schema/prisma.graphql
    extensions:
      prisma: prisma/prisma.yml
